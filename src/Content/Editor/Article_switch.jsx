import PropTypes from "prop-types";

function Article_switch({ id }) {
  // console.log(`🌸Article_switch🌸`)

  return <div>{id}</div>;
}

export default Article_switch;

Article_switch.propTypes = { prop: PropTypes.any };
