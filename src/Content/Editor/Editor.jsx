import PropTypes from "prop-types";

function Editor({ prop }) {
  // console.log(`🌸Editor🌸`)

  return <div>Editor</div>;
}

export default Editor;

Editor.propTypes = { prop: PropTypes.any };
